<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('includes/head') %> 
  <link rel="stylesheet" href="styles/shared.css" />
  <link rel="stylesheet" href="styles/restaurants.css" />
  <link rel="stylesheet" href="styles/recommend.css" />
</head>

<body>
  <!-- Header -->
  <%- include('includes/header') %> 

  <main>
    <h1>Recommended restaurants</h1>
    <% if (numberOfRestaurants === 0) { %>
      <p>Unfortunately, We have no Restaurants yet. Why don't you recommend some</p>
      <p>Click below to do so</p>
      <a href="/recommend"><button>Share a Restaurant</button></a>
    <% } else { %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <p>We found a total of <strong><%= numberOfRestaurants %></strong> Restaurants</p>
      <ul id="restaurants-list">
        <% for (const restaurant of restaurants){ %>
        <li class="restaurant-item">
          <article>
            <h2><%= restaurant.name %> </h2>
            <div class="restaurant-meta">
              <p><%= restaurant.cuisine %> </p>
              <p><%= restaurant.address %> </p>
            </div>
            <p>
              <%= restaurant.description %>
            </p>
            <p><i><%= restaurant.username %> </i></p>
            <div class="restaurant-actions">
              <a href="<%= restaurant.website %>">Visit Site </a>
            </div>
          </article>
        </li>
        <% } %>
      </ul>
  
    <% } %>
    
  </main>
</body>

</html>